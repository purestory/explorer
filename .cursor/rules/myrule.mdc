---
description: 
globs: 
alwaysApply: true
---

# Your rule content

- You can @ files here
- You can use markdown but dont have to


깃허브 주소 : https://github.com/purestory/webdav

frontend서버와 backend서버를 동시에 운영하고 있다.
frontend는 nginx로 구동되고 있다.

깃업로드시 nginx 설정 저장

너는 서버컴퓨터의 관리자로서, 컴퓨터 전체를 컨트롤 할수 있다.


시스템 파일을 운용할때는 sudo명령을 사용하고, 시스템 설정을 수정할때는 별도 파일을 생성해서 sudo 복사하는 방식으로 수정한다.

서비스를 중지했을때는 반드시 중지가 되었는지 확인한다.
서비스를 구동했을때는 반드시 구동이 되었는지 확인닿.


2000줄이상 파일 작업시 함수 단위로 전체 코드를 제공해 주면 직접 수정할께


2000줄이상의 파일 작업시 util 폴더에 있는 프로그램을 사용한다.
- `extract_for_edit.py`: 원본 파일을 복사하고 수정할 부분을 별도의 파일로 추출합니다. 
- `insert_edited_part.py`: 사용자가 수정한 부분을 다시 복사본 파일에 삽입합니다.
1.  **추출 단계:** `extract_for_edit.py`를 사용하여 원본 파일을 복사하고 수정할 라인 범위를 별도 파일로 만듭니다. 수정할 라인 범위는 해당 함수 전체로 한다.
2.  **수정 단계:** `util/<원본 파일명>.part_to_edit` 파일을 텍스트 편집기로 열어 원하는 대로 수정합니다. 수정 후에는 반드시 제대로 수정했는지 확인 후 보고한다.
3.  **삽입 단계:** `insert_edited_part.py`를 사용하여 수정된 내용을 복사본 파일에 다시 합칩니다.
4.  **반영 단계:** 수정이 완료된 `util/<원본 파일명>.copy` 파일을 **직접** 원래의 `<원본 파일 경로>`로 복사하여 변경 사항을 최종 반영합니다. 시스템 파일 등 권한이 필요한 경우 `sudo`를 사용하여 복사합니다.
